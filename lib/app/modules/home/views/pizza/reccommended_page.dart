import 'package:flutter/material.dart';

class ReccommendedPage extends StatelessWidget {
  const ReccommendedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Reccommended'),
    );
  }
}
