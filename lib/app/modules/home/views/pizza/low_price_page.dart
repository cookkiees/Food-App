import 'package:flutter/material.dart';

class LowPricePage extends StatelessWidget {
  const LowPricePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Low Price'),
    );
  }
}
